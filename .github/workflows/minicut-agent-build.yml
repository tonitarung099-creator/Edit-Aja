name: Build MiniCut Studio Agent

on:
  workflow_dispatch:
  push:
    branches:
      - minicut-agent-build
    paths:
      - "tools/minicut-agent/**"
      - ".github/workflows/minicut-agent-build.yml"

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: tools/minicut-agent

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Compile and import checks
        shell: pwsh
        run: |
          python -m compileall -q minicut_agent main.py mcp_server.py
          python -c "from minicut_agent.ui import MiniCutWindow; print('ui-import-ok')"
          python -c "import mcp_server; print('mcp-import-ok')"

      - name: Build MiniCut Studio Agent
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --windowed --name "MiniCut Studio Agent" --hidden-import PySide6.QtMultimedia --hidden-import PySide6.QtMultimediaWidgets main.py

      - name: Build MCP companion
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --console --name "MiniCut MCP" mcp_server.py
          Copy-Item "dist/MiniCut MCP/MiniCut MCP.exe" "dist/MiniCut Studio Agent/MiniCut MCP.exe"
          Copy-Item README.md "dist/MiniCut Studio Agent/README_AGENT.md"

      - name: Smoke test executable
        shell: pwsh
        run: |
          $p = Start-Process -FilePath "dist/MiniCut Studio Agent/MiniCut Studio Agent.exe" -ArgumentList "--self-test" -PassThru
          if (-not $p.WaitForExit(30000)) {
            $p.Kill()
            throw "MiniCut self-test timeout"
          }
          $p.Refresh()
          if ($p.ExitCode -ne 0) {
            throw "MiniCut self-test failed with exit code $($p.ExitCode)"
          }

      - name: Package ZIP
        shell: pwsh
        run: |
          Compress-Archive -Path "dist/MiniCut Studio Agent/*" -DestinationPath "MiniCut-Studio-Agent-Windows-Lite.zip" -Force

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: MiniCut-Studio-Agent-Windows-Lite
          path: tools/minicut-agent/MiniCut-Studio-Agent-Windows-Lite.zip
          if-no-files-found: error
